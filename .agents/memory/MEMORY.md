- [Index-based settings callbacks](index-based-settings-callbacks.md) — alias_generator removed from all models; callbacks use positional int index, not field names or hashes.
- [Webhook vs polling mode](webhook-polling-mode.md) — TelegramModel.no_updates controls mode; True=webhook (default), False=MTProto polling. WebsModel.telegram_secret_token secures webhook header.
- [Token prefix vs actual bot ID mismatch](token-vs-me-id-mismatch.md) — BOT_TOKEN prefix (node_id) can differ from Pyrogram me.id after auth; _setup_main_bot must sync _main_bot_id after client.start().
- [migrate command usage](migrate-command-usage.md) — /migrate -n takes the NEW node_id; must run BEFORE swapping BOT_TOKEN so get_node_id() still equals the old value.
- [Cancel task await pattern](cancel-task-await.md) — inner wait_for_message cancel tasks must be awaited after cancel() or stale handlers block next user input.
- [Node heartbeat upsert datetime](node-heartbeat-datetime.md) — NodeStatusCollection.upsert must use mode="python" not mode="json"; json mode stores datetime as ISO string breaking BSON DateTime and age calculation.
- [Bot reassignment order](bot-reassignment-order.md) — update DB node_id FIRST (triggers Change Stream on target), then stop_client + unregister_client locally. Use unregister_client not remove_client (latter deletes DB doc).
- [MongoDB session storage](mongo-storage.md) — all persistent clients use MongoStorage (pyrogram DB) not SQLite files; eliminates AuthKeyDuplicated across VPS moves.
- [MongoStorage perf patterns](mongo-storage-perf.md) — _session_cache primes at open(); _indexes_ensured flag; update_peers parallelised; inspect.stack() replaced with explicit attr param; force_sub guard in both role.py and service.py.
- [Peers vs bot_users for broadcast targeting](peers-vs-bot-users.md) — Chat Cloner uses db_manager.pyrogram.iter_peers/get_peer_count for all -dc user/bot/channel/supergroup/group targets. bot_users kept only for is_new_user in start_menu/XMissions.
- [Chat Cloner -all flag replaces allusers](chat-cloner-all-flag.md) — BroadcastTarget.ALLUSERS removed; ArgFlags.ALL (-all) applies to any peer target for all-clients broadcast (dev only, 3x cost). PeerType StrEnum in tasks.py for typed peer strings.
- [Pyrogram session storage — pyrogram DB](pyro-session-storage.md) — single shared collections + composite _id keys + compound indexes; kurigram PROD/TEST DC map; DB name derived from module name via __name__.rsplit(".", 1)[-1].
- [Managed bot token retrieval](managed-bot-token-retrieval.md) — use client.get_managed_bot_token(bot_id) — native kurigram MTProto — never HTTP; replace_managed_bot_token() revokes+regenerates.
- [kurigram migration from pyrotgfork](kurigram-migration.md) — both install as pyrogram namespace; one handler rename; send_code aliased; full API diff notes.
- [kurigram webhook adapter API diffs](kurigram-webhook-adapter-api-diffs.md) — all constructor kwarg renames (User/Message/Photo/Sticker/ChatJoinRequest) + filter compatibility patch for CallbackQuery.
- [Rich message MessageTooLong handler](rich-message-too-long.md) — _safe_execute must extract text from rich_message kwarg (not positional args); EditMessage TL supports rich_message at flag.23.
- [asyncio.wait requires Tasks on Python 3.11+](asyncio-wait-tasks.md) — pass create_task(coro) not bare coro; cancel pending tasks in finally to avoid leaks.
- [Redis access cache invalidation sites](redis-access-cache-invalidation.md) — use access_cache.invalidate_many(client_id, user_ids) for bulk evictions; import access_cache singleton from utils.redis_peer_cache; get_chat_members limit=0 to get all members.
- [Pre-composed private_bot / private_user filters](private-bot-filter-constants.md) — use filters.private_bot / private_user constants, never inline filters.private & filters.bot_client.
- [_safe_execute retry depth guard](safe-execute-depth-guard.md) — MediaCaptionTooLong/ReplyMarkupInvalid retries re-enter _safe_execute(depth+1); bare call only at depth>=1 to cap recursion.
- [Pyrogram batch API patterns](pyrogram-batch-api-patterns.md) — get_messages/delete_messages accept list[int]; always use batch form inside loops to avoid N sequential MTProto round-trips.
- [TOCTOU claim-first / reward-second](toctou-claim-reward-order.md) — insert claim/mark_rewarded BEFORE grant_reward; DuplicateKeyError/modified_count==0 is the race guard; rollback on grant failure.
- [IndexedCollection secondary-index lock pattern](indexed-collection-lock-pattern.md) — lock before old_model snapshot; lock for add+remove swap; load() index rebuild holds lock; subclasses override _apply_remote_change. Rounds 15–16.
- [API endpoint error hygiene](api-error-hygiene.md) — handler guard pattern; bare except: forbidden; LOGGER.exception before every 500 raise; no str(e) in responses; v1/v2/public complete as of Round 22 (2026-06-25).
- [database→backend import direction](database-backend-import-direction.md) — database/ must never import from backend/; crashes uvicorn with circular import. Constants go in database/constants.py; bot-layer deps in expiration.py use lazy @property. Cross-layer callbacks use hook-registration pattern (backend pushes callable into collection at startup).
- [Structured log hygiene](structured-log-hygiene.md) — LOGGER.exception uses plain string (no {e}, exc_info auto-captured); LOGGER.warning/error/debug uses error=str(e) kwarg; static event strings required for log aggregation.
- [Pydantic sensitive field repr=False](pydantic-sensitive-field-repr.md) — use repr=False not exclude=True on secret fields; exclude=True breaks model_dump() → MongoDB persistence silently.
- [Active sweep findings](sweep-2026-07-08-findings.md) — 2026-07-08 sweep: 104 findings; ~51 resolved across sessions; still-open list at bottom of file.
- [2026-07-11 sweep](sweep-2026-07-11-findings.md) — 20 findings; CI-I1+MAINT-B3 fixed 2026-07-11; open: SEC-W2, SCALE-W4, DI-F1, DI-F2, SEC-A1, MAINT-A2, CORR-B4, PERF-D2.
- [Google dep footprint](google-dep-footprint.md) — aiogoogle is sole client; googleapis-common-protos/proto-plus/protobuf are orphaned and removed; uritemplate must stay pinned (runtime dep undeclared in aiogoogle metadata).
- [Always-skip recommendations](always-skip-recs.md) — SEC-1 (Saweria webhook sig) and X3 (Pydantic UpdateParser) are intentionally deferred; never resurface as next-work items.
- [Library upgrade audit process](library-upgrade-audit-process.md) — use `library-upgrade-audit` skill + `docs/superpowers/library-usage.md` to cross-reference changelogs against real usage patterns.
- [Replit npm lockfile registry](replit-npm-lockfile.md) — RESOLVED 2026-07-03: Replit no longer proxies npm; package-lock.json is committed and CI uses npm ci + cache again.
- [Project conventions](project-conventions.md) — v1/v2 API split is intentional (JWT vs cookie auth), easy to maintain; Python 3.14 is the confirmed runtime, do not suggest downgrading.
- [aiogoogle migration](aiogoogle-migration.md) — all Google API code on aiogoogle==5.17.0; GDriveCredentials pickles; proactive token refresh in _authorize_with_token; no inter-page sleep in paginated lists.
- [Dependency management migration](dependency-management.md) — canonical deps are now pyproject.toml + uv.lock; requirements.txt is deprecated; use `uv sync` not `pip install -r`; redis 8.x has no [asyncio] extra.
- [pytest ASGI test client setup](pytest-asgi-setup.md) — three non-obvious gotchas when testing this FastAPI app with httpx+ASGITransport.
- [Plugin manifest system](plugin-manifest-system.md) — YAML sidecars in bot/plugins/ declare update_types; set_webhook builds allowed_updates from them; loader is lazy-cached.
- [Request tracing / X-Request-ID](request-tracing.md) — outermost middleware; clear_contextvars() before bind to prevent keep-alive context leakage; echoed on response.
- [bot/utils mixed real-code vs shims](bot-utils-real-code.md) — bot/utils/links/ has no __init__.py; import validators.py directly; fmt.py is real code not a shim.
- [X2 process-split cancelled](x2-process-split-cancelled.md) — splitting bot+backend into separate processes was cancelled; dev workflows cover independence; prod restart-all via update.py is intentional.
- [Frontend use-api.js fetch migration](frontend-fetch-migration.md) — axios removed; native fetch; external interface (get/post/put/createEventSource + error.response shape) preserved; do not re-introduce axios.
- [CI pipeline](ci-pipeline.md) — ci.yml on dev push/PR; two jobs: pytest+MongoDB and frontend build; release-please gated on CI via workflow_run.
- [DEEP_LINK_SECRET / XOR hardening](deep-link-crypto.md) — required to harden MsgStore/market XOR tokens; without it key=public bot_id (forgeable). All VPS nodes must share the same value. Token rotation needed on first-time set.
- [Replit uv env var overrides](replit-uv-env.md) — UV env vars now in .replit [userenv.shared] (not start.sh); setup_venv() runs uv python install 3.14.6 before uv venv; PYTHONPATH="" clears Nix pollution.
- [sys.executable vs bare python3](sys-executable-rule.md) — always use sys.executable for subprocess calls inside the bot; bare "python3" resolves to Nix 3.12 on Replit, not the venv.
- [Unified start.sh entrypoint](unified-start-sh.md) — all workflows route through bash start.sh [--mode]; modes: prod/dev/build/lint/test/init/clean; UV exports live in .replit, not start.sh.
- [BaseCollection LRU mode](base-collection-lru.md) — four archetypes; unified Cache(maxsize,ttl) for peers/access; get_all/get_ids/snapshots removed; ProductsCollection+InventoryCollection+RssCollection indexes now active.
- [Collection cache archetype mismatches](collection-cache-archetypes.md) — missions Readme said "Full dict" but is direct-DB (no load); records+tasks use IndexedCollection with no registered indexes (should be BaseCollection); missions Cache(max_size) wrong — needs per-bot TTL dict, not BaseCollection LRU.
- [MsgStore chain-mode and album delivery](msgstore-chain-and-album-delivery.md) — chain relay must re-check paid content per hop; album delivery groups by contiguous media_group_id runs, not all-or-nothing.
- [Sweep verification pitfalls](sweep-verification-pitfalls.md) — finally-blocks run on success too, Path.with_*() is non-mutating, mutually-exclusive branches aren't duplicates, dedup caches need bot_id in multi-bot setups.
- [Retroactive commit message rewrite](commit-message-rewrite-tool.md) — reword non-conventional commit batches via git filter-branch --msg-filter keyed on SHA, not interactive rebase; template at `scripts/rewrite-commit-messages.sh`.
- [httpx shared client pattern](http-shared-client.md) — verify=True calls share a pooled AsyncClient; verify=False must use throwaway client (httpx fixes verify at construction). Call close_http_clients() at shutdown.
- [WalletModel Decimal/float arithmetic](wallet-decimal-float.md) — WalletModel.balance is Decimal; GlobalConfig.min_wallet_balance and locked-balance dict are float; must use float(balance) before any arithmetic.
- [get_dialogs offset_date unsupported](get-dialogs-offset.md) — kurigram get_dialogs() accepts only limit/exclude_pinned/from_archive; offset_date is internal; pass limit=seen+100 to paginate deeper.
- [Service force-kill self-PID guard](service-self-kill-guard.md) — BaseService kill_by_pid/pkg/port must exclude os.getpid(); in-process WebServerService restart could self-SIGTERM the whole bot.
- [Partial-fix review pitfalls](partial-fix-review-pitfalls.md) — persisting raw input after validation, deleting on existence-only checks, and scheme-truthiness for URL parsing are recurring "looks fixed but isn't" patterns; always verify with concrete inputs.
- [Dependency vuln fix patterns](dependency-vuln-remediation.md) — for unfixable transitive vulns, bump the parent dep that may have dropped it instead of waiting on the leaf package.
- [Sweep doc ✅ claims can be false](sweep-doc-false-fixed-claims.md) — N-30 was marked "Fixed" with code that was never committed; always grep for the claimed symbol before trusting a sweep-doc status.
- [Sweep "Fixed" can be real but incomplete](sweep-partial-fix-patterns.md) — code backing a Fixed row can genuinely exist and still be wrong; verify behavior via review, not just presence, before trusting the row.
- [LRU lock-table eviction race](lru-lock-table-eviction-race.md) — capping a per-key asyncio.Lock table with LRU eviction can evict a currently-held lock, letting a new caller for the same key skip waiting entirely.
- [Stat-cache invalidation on mutation](stat-cache-invalidation-on-mutation.md) — a TTL-only exists()/is_dir() cache goes stale immediately after a write/delete on the same path within the TTL window; must invalidate on mutating ops.
- [Duplicate Pydantic before-validators](pydantic-duplicate-validators.md) — two @field_validator(mode="before") on the same field both run silently, no error/warning; grep before adding a new one.
- [base-image dependency sync](base-image-dependency-sync.md) — base-image/pyproject.toml+uv.lock must mirror root exactly or deployed image drifts; enforced by check-base-image-sync CI job.
- [aiopath standalone library setup](aiopath-standalone-library.md) — setuptools package-dir trick for flat-layout sub-packages, uv workspace wiring, and an AsyncPath super() vs self._path bug pattern.
- [IS_TEST_MODE replaces test_mode field](is-test-mode-canonical.md) — test_mode field removed from GlobalModel and WebsConfig; all callers use IS_TEST_MODE from utils.constants directly.
- [CollectionModel config inheritance](collection-model-config.md) — model_config is defined only on CollectionModel/base.py; all subclasses (DaemonModel, UtilityModel, and every leaf model) inherit it — never re-declare it.
- [update.py minimal git refresh](update-py-git-refresh.md) — hard_reset resets onto FETCH_HEAD after a depth=1 fetch, not a named ref; no .git.bak, no branch-history clone.
- [Task/command unit testing without live Telegram](task-command-unit-testing.md) — fake Client/Message + mock configure_task; circular import gotcha; -d seed flag is boolean-only (ratio:time code is dead).
